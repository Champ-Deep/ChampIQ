[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/src/cli/index.js serve"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
