"""Orchestrator core — abstractions only.

This module defines the Interface Segregation + Dependency Inversion layer.
Concrete implementations live in sibling packages (credentials/, expressions/,
drivers/, nodes/, runtime/) and are wired together by the orchestrator.
"""
